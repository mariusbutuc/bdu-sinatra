source :rubygems

gem 'sinatra'
gem 'data_mapper'
gem 'haml'
gem 'shotgun'
gem 'thin'

group :production do
    gem 'pg'
    gem 'dm-postgres-adapter'
end

group :development, :test do
    gem 'sqlite3'
    gem 'dm-sqlite-adapter'
    gem 'tux'
end
